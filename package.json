{
  "name": "gas-template",
  "version": "1.0.0",
  "description": "",
  "main": "build/backend.js",
  "private": true,
  "engines": {
    "node": ">=14",
    "yarn": ">=1.22"
  },
  "scripts": {
    "open": "clasp open",
    "build": "node build.js",
    "buildpush": "cp appsscript.json build/appsscript.json && yarn run build && clasp push --force",
    "postinstall": "mkdir -p build && cp appsscript.json build/appsscript.json"
  },
  "dependencies": {},
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.57",
    "@typescript-eslint/eslint-plugin": "^5.49.0",
    "@typescript-eslint/parser": "^5.49.0",
    "esbuild": "^0.20.1",
    "esbuild-gas-plugin": "^0.8.0",
    "eslint": "^8.32.0",
    "eslint-plugin-import": "^2.27.5",
    "typescript": "^4.9.4"
  }
}
